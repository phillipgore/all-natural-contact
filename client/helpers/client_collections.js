/* --------- Infinite Scroll  ---------- */
InfiniteScroll = new Mongo.Collection(null);
ConversationInfiniteScroll = new Mongo.Collection(null);
TagInfiniteScroll = new Mongo.Collection(null);

/* --------- Multi Select  ---------- */
ContactSelect = new Mongo.Collection(null);
ConversationSelect = new Mongo.Collection(null);
TagSelect = new Mongo.Collection(null);

/* --------- Export Data ---------- */
selectedContacts = new Mongo.Collection(null);
selectedTags = new Mongo.Collection(null);

/* --------- Search Data ---------- */
FoundContacts = new Mongo.Collection(null);
